version: "3.9"
name: "hjemmeside"

services:
  frontend:
    image: spedsbjerg/spedsbjergwebsite:latest
    restart: always
    ports:
      - "8080:8080"
  watchtower:
    image: containrrr/watchtower
    restart: always
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    command: --interval 120 frontend